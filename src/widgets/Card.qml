import QtQuick
import Quickshell.Widgets
import qs.theme

// A section of a panel: one layout, inset and sitting on a slightly lighter
// ground than the slab.
WrapperRectangle {
    color: Theme.surface
    radius: Theme.cardRadius
    margin: Theme.cardPadding
}
